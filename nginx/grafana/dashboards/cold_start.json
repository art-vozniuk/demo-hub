{
  "annotations": {"list": []},
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "type": "timeseries",
      "title": "Cold-start rate",
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "gridPos": {"h": 7, "w": 12, "x": 0, "y": 0},
      "id": 1,
      "targets": [
        {"expr": "sum by (config) (rate(demo_hub_flux_cold_start_duration_seconds_count[5m]))",
         "legendFormat": "{{config}}", "refId": "A"}
      ],
      "fieldConfig": {"defaults": {"unit": "ops"}, "overrides": []}
    },
    {
      "type": "timeseries",
      "title": "Cold-start p95 by phase (s)",
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "gridPos": {"h": 7, "w": 12, "x": 12, "y": 0},
      "id": 2,
      "targets": [
        {"expr": "histogram_quantile(0.95, sum by (le, config, phase) (rate(demo_hub_flux_cold_start_duration_seconds_bucket[10m])))",
         "legendFormat": "{{config}} / {{phase}}", "refId": "A"}
      ],
      "fieldConfig": {"defaults": {"unit": "s"}, "overrides": []}
    },
    {
      "type": "timeseries",
      "title": "Container uptime ($/hr equivalent)",
      "description": "Sum of seconds × rate gives an instantaneous spend velocity per config.",
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "gridPos": {"h": 7, "w": 24, "x": 0, "y": 7},
      "id": 3,
      "targets": [
        {"expr": "sum by (config, gpu) (rate(demo_hub_flux_container_uptime_seconds_total[1m]))",
         "legendFormat": "{{config}} / {{gpu}}", "refId": "A"}
      ],
      "fieldConfig": {"defaults": {"unit": "short"}, "overrides": []}
    }
  ],
  "refresh": "10s",
  "schemaVersion": 39,
  "tags": ["cold-start", "inference"],
  "templating": {"list": []},
  "time": {"from": "now-1h", "to": "now"},
  "timepicker": {},
  "timezone": "browser",
  "title": "Cold Start Analysis",
  "uid": "cold-start",
  "version": 1,
  "weekStart": ""
}
