[project]
name = "demo-hub-modal"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "modal>=1.4.2",
]

[tool.pytest.ini_options]
# So `pytest tests/` resolves the local source packages (transcriber_pipeline,
# common) no matter how pytest was invoked.
pythonpath = ["."]
