name: build and push dispatch

on:
  workflow_dispatch:
  workflow_call:

jobs:
  lint:
    uses: ./.github/workflows/lint-python.yml

  test:
    uses: ./.github/workflows/test-dispatch.yml

  build-and-push:
    needs: [lint, test]
    if: ${{ success() }}
    runs-on: ubuntu-latest

    steps:
      - name: checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: create env file
        run: |
          cat > /tmp/dispatch.env << EOF
          ENV=${{ secrets.ENV }}
          SENTRY_DSN=${{ secrets.SENTRY_DSN }}
          S3_ACCESS_KEY_ID=${{ secrets.S3_ACCESS_KEY_ID }}
          S3_ACCESS_KEY_SECRET=${{ secrets.S3_ACCESS_KEY_SECRET }}
          S3_ENDPOINT=${{ secrets.S3_ENDPOINT }}
          S3_PUBLIC_BUCKETS_ENDPOINT=${{ secrets.S3_PUBLIC_BUCKETS_ENDPOINT }}
          S3_REGION=${{ secrets.S3_REGION }}
          RABBITMQ_URL=amqp://${{ secrets.RABBITMQ_USER }}:${{ secrets.RABBITMQ_PASSWORD }}@${{ secrets.SERVER_HOST }}:5672/
          MODAL_GENERATIVE_SUBMIT_URL=${{ secrets.MODAL_GENERATIVE_SUBMIT_URL }}
          MODAL_GENERATIVE_POLL_URL=${{ secrets.MODAL_GENERATIVE_POLL_URL }}
          MODAL_SHARP_SUBMIT_URL=${{ secrets.MODAL_SHARP_SUBMIT_URL }}
          MODAL_SHARP_POLL_URL=${{ secrets.MODAL_SHARP_POLL_URL }}
          MODAL_PROXY_AUTH_TOKEN_ID=${{ secrets.MODAL_PROXY_AUTH_TOKEN_ID }}
          MODAL_PROXY_AUTH_TOKEN_SECRET=${{ secrets.MODAL_PROXY_AUTH_TOKEN_SECRET }}
          EOF

      - name: login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: set up docker buildx
        uses: docker/setup-buildx-action@v3

      - name: build and push docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: services/dispatch/Dockerfile
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/dispatch:latest
          platforms: linux/amd64
          cache-from: type=gha
          cache-to: type=gha,mode=max
          secret-files: |
            env=/tmp/dispatch.env
